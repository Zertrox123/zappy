[package]
name = "game"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "zappy_server"
path = "src/main.rs"

[dependencies]
server = { path = "../server" }
rand = "0.8"

[dev-dependencies]
server = { path = "../server" }
